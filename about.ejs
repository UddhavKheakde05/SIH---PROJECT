<%- include('header.ejs') %>

    <div class="main-content">
        <div class="container">
            <h2>🌱 About Us</h2>
            <p>EcoLearn is a gamified learning platform for Classes 6–12.</p>
            <p>Our mission is to make education fun, accessible, and interactive.</p>
        </div>
    </div>

    <!-- JS logic -->
    <script>
        document.addEventListener('DOMContentLoaded', async () => {
            const aboutTitle = document.querySelector('.container h2');
            const paragraphsContainer = document.querySelector('.container');

            try {
                const response = await fetch('http://localhost:5000/api/about');
                const data = await response.json();

                // Replace title
                aboutTitle.textContent = data.title;

                // Clear and rebuild paragraphs
                paragraphsContainer.innerHTML = `<h2>${data.title}</h2>`;
                data.paragraphs.forEach(p => {
                    const paragraph = document.createElement('p');
                    paragraph.textContent = p;
                    paragraphsContainer.appendChild(paragraph);
                });
            } catch (error) {
                console.error("Error fetching about data:", error);
            }
        });
    </script>

    <script defer src="about.js"></script>

    <%- include('footer.ejs') %>